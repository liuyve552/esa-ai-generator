# ---- AI (Text) ----
# Prefer Qwen (DashScope). If missing, the app uses a safe mock/template fallback.
DASHSCOPE_API_KEY=
AI_TEXT_MODEL=qwen-max

# ---- AI (Image, optional) ----
AI_IMAGE_MODEL=

# ---- Optional: override free geo provider ----
# GEO_PROVIDER=ipwhois

